[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --tb=short
asyncio_mode = auto
markers =
    integration: requires an external integration service such as PostgreSQL
    slow: exercises a local index or other comparatively expensive workflow
