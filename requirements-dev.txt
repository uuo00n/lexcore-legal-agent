# 测试专用依赖（与 requirements.txt 分开，方便生产部署不带它们）
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27        # FastAPI TestClient 在新版用 httpx

# 评测系统（eval/run_eval.py e2e 模式）
ragas>=0.2

# 仓储层单测用内存库，避免测试依赖真实 PostgreSQL
aiosqlite>=0.20
