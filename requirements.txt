fastapi>=0.115
uvicorn[standard]>=0.30
gunicorn>=22.0
python-multipart>=0.0.9
sse-starlette>=2.1
python-dotenv>=1.0
httpx>=0.27
pydantic>=2.0

langgraph>=1.0
langgraph-checkpoint-postgres>=3.1
psycopg[binary,pool]>=3.2
langchain-core>=0.3
langchain-openai>=0.2
langchain-text-splitters>=0.3

# MCP 工具协议
# FastMCP v1 暴露层；MCP Python SDK v2 已移除该类。
mcp>=1.9,<2

pypdf>=5.0
python-docx>=1.1
Pillow>=10.0

# RAG 向量存储
chromadb>=0.5
qdrant-client>=1.12
pymilvus>=2.4

# Embedding & Rerank
sentence-transformers>=3.0
transformers>=4.40

# 关键词检索
rank-bm25>=0.2.2

# PostgreSQL 持久化（infrastructure/）
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14
asyncpg>=0.30

# Redis 缓存 / 限流 / 会话元数据 / 幂等（infrastructure/redis.py、services/cache/）
redis>=5.0
